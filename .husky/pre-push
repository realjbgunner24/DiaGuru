#!/usr/bin/env sh
# Block pushes to main; require a PR
while read local_ref local_sha remote_ref remote_sha
do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct pushes to main are disabled. Open a PR." >&2
    exit 1
  fi
done
exit 0

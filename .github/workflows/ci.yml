name: CI
on:
  pull_request:
    branches: ["main"]
    paths:
      - 'dia-guru-reboot/**'
      - '.github/workflows/ci.yml'
  push:
    branches: ["main"]
    paths:
      - 'dia-guru-reboot/**'
      - '.github/workflows/ci.yml'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dia-guru-reboot
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: dia-guru-reboot/package-lock.json
      - run: npm ci
      - run: npm run lint --if-present

  typecheck:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dia-guru-reboot
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: dia-guru-reboot/package-lock.json
      - run: npm ci
      - run: npm run typecheck --if-present

  policy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Direct pushes to main are disallowed. Use PRs." >&2
          exit 1
